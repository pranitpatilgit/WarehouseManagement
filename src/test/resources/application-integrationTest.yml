server:
  port: 8091
spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb


application:
  inventory.path: ${PWD}/src/test/resources/data/inventory.json
  product.path: ${PWD}/src/test/resources/data/products.json